# LHScan
这是一个用于扫描是否有dll劫持的扫描器  
目的是用于扫描系统中是否存在可用的dll劫持，目前做了一些杀软的规避，不过只是代码静态层面，后续可能会改进。  
-h 查看使用说明  
-r 输入一个程序的全路径，启动这个程序并开启监控这个程序静态和动态导入的dll，以及路径是否有可以劫持的dll  
-p 输入一个已经启动进程的pid，监控这个进程静态和动态导入的dll，以及路径是否有可以劫持的dll  
例：LHScan.exe -r C:\Windows\System32\calc.exe 或者 LHScan.exe -p 计算器的pid  
